// app.get('/survey', function(req,res) {
//     connection.query('SELECT * FROM snuggleseeker;', function(err, data) {
//       if (err) throw err;

//       res.render('survey', {survey: data});

//     });
// });

// app.use(express.static(__dirname + '/public'));